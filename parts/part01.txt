<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Cabbage 植粹膠囊｜植物型日常營養配方研發與 B2B 商品提案</title>
<meta name="description" content="Cabbage 植粹膠囊以高麗菜為核心，結合纖維底盤與多原料植物配伍，建立有結構、有規格、有技術來源的植物型日常營養配方與 B2B 商品研發提案。">
<meta name="robots" content="index,follow">
<meta name="theme-color" content="#14362f">
<link rel="canonical" href="https://gcmgcm2021-lang.github.io/-dear-zi-yan/">
<style>
:root{
  --bg:#071915;
  --bg2:#0c241e;
  --panel:#102c25;
  --panel2:#f7faf8;
  --green:#2A9D8F;
  --green-dark:#1D7A6E;
  --green-soft:#D4E9E2;
  --ivory:#F6FBF9;
  --gold:#C8A45D;
  --gold2:#e0bf75;
  --ink:#17231f;
  --muted:#71817b;
  --line:#dce8e3;
  --warning:#A65B17;
  --warning-bg:#fff7ed;
  --white:#fff;
  --shadow:0 22px 70px rgba(2,18,14,.15);
  --radius:26px;
  --container:1180px;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family:"Noto Sans TC","PingFang TC","Microsoft JhengHei",system-ui,sans-serif;
  color:#34433e;
  background:#f6fbf9;
  line-height:1.75;
}
a{color:inherit}
img{display:block;max-width:100%}
.container{width:min(calc(100% - 40px),var(--container));margin:auto}
.section{padding:92px 0;scroll-margin-top:74px}
.section-light{background:#fff}
.section-soft{background:#f6fbf9}
.section-dark{
  background:
    radial-gradient(circle at 15% 0%,rgba(42,157,143,.18),transparent 28%),
    radial-gradient(circle at 92% 18%,rgba(200,164,93,.09),transparent 24%),
    linear-gradient(145deg,#0a211b,#071713);
  color:#dbe7e2;
}
.section-dark h2,.section-dark h3{color:#fff}
.kicker{
  display:inline-flex;align-items:center;gap:8px;
  color:var(--green-dark);font-size:.82rem;font-weight:900;
  letter-spacing:.14em;text-transform:uppercase;margin-bottom:12px
}
.section-dark .kicker{color:#77c7b8}
h1,h2,h3{color:var(--ink);line-height:1.18;margin-top:0}
h1{font-size:clamp(2.9rem,7vw,6rem);letter-spacing:-.055em;margin-bottom:16px}
h2{font-size:clamp(2rem,4vw,3.5rem);letter-spacing:-.035em;margin-bottom:18px}
h3{font-size:1.25rem}
.lead{font-size:clamp(1.06rem,1.7vw,1.32rem);max-width:820px;color:#53625d}
.section-dark .lead{color:#bfd1ca}
.gold{color:var(--gold2)}
.green{color:var(--green)}
/* nav */
.topbar{
  position:sticky;top:0;z-index:50;
  backdrop-filter:blur(18px);
  background:rgba(246,251,249,.92);
  border-bottom:1px solid rgba(220,232,227,.9)
}
.nav{display:flex;align-items:center;justify-content:space-between;gap:18px;min-height:66px}
.brand{font-weight:900;color:#153e34;text-decoration:none;white-space:nowrap}
.navlinks{display:flex;gap:4px;overflow-x:auto;scrollbar-width:none}
.navlinks::-webkit-scrollbar{display:none}
.navlinks a{font-size:.84rem;text-decoration:none;color:#5d6c67;padding:8px 10px;border-radius:999px;white-space:nowrap}
.navlinks a:hover,.navlinks a.active{background:#dbeee8;color:#165d50}
/* buttons */
.buttons{display:flex;flex-wrap:wrap;gap:12px;margin-top:28px}
.btn{display:inline-flex;align-items:center;justify-content:center;min-height:48px;padding:0 22px;border-radius:999px;font-weight:900;text-decoration:none;transition:.2s}
.btn-primary{background:var(--green-dark);color:#fff}
.btn-primary:hover{transform:translateY(-2px);box-shadow:0 12px 28px rgba(29,122,110,.24)}
.btn-outline{border:1px solid var(--green);color:var(--green-dark);background:#fff}
/* hero */
.hero{
  position:relative;overflow:hidden;
  background:
    radial-gradient(circle at 90% 20%,rgba(42,157,143,.25),transparent 24%),
    radial-gradient(circle at 12% 70%,rgba(200,164,93,.12),transparent 28%),
    linear-gradient(145deg,#071a16,#0e3129 54%,#071813);
  color:#fff;padding:86px 0 96px
}
.hero::after{
  content:"";position:absolute;inset:0;pointer-events:none;opacity:.16;
  background-image:
    linear-gradient(rgba(255,255,255,.06) 1px,transparent 1px),
    linear-gradient(90deg,rgba(255,255,255,.06) 1px,transparent 1px);
  background-size:52px 52px;
  mask-image:linear-gradient(to bottom,black,transparent 85%)
}
.hero-grid{position:relative;z-index:1;display:grid;grid-template-columns:1.05fr .95fr;gap:52px;align-items:center}
.hero h1{color:#fff}
.hero .lead{color:#ceddd7}
.hero-badge{display:inline-block;padding:7px 13px;border:1px solid rgba(123,201,187,.4);color:#9ad8cc;border-radius:999px;font-weight:800;font-size:.82rem;margin-bottom:18px}
.hero-meta{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:28px}
.hero-meta>div{padding-top:12px;border-top:1px solid rgba(255,255,255,.17);font-size:.9rem}
.hero-meta strong{display:block;color:#8ed1c5;font-size:.78rem;margin-bottom:2px}
/* image */
.media{
  width:100%;background:#fff;border:1px solid rgba(218,230,225,.9);
  border-radius:var(--radius);padding:16px;box-shadow:var(--shadow)
}
.media img{
  width:100%;height:auto!important;object-fit:contain!important;object-position:center!important;
  background:#fff;border-radius:calc(var(--radius) - 10px)
}
.media-caption{font-size:.78rem;color:#7b8a85;text-align:center;margin-top:10px}
.hero .media{background:rgba(255,255,255,.96);border-color:rgba(255,255,255,.22)}
/* layout */
.two{display:grid;grid-template-columns:1fr 1fr;gap:36px;align-items:center}
.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
.cards.four{grid-template-columns:repeat(4,1fr)}
.card{
  background:#fff;border:1px solid var(--line);border-radius:18px;padding:22px;
  box-shadow:0 10px 30px rgba(20,70,60,.05)
}
.section-dark .card{background:rgba(255,255,255,.055);borde